// Returns a header with the name of the app

const Header = () => {
    return (
        <header class="header">
            <h1>Disaster Tracker (Live NASA EONET Data)</h1>
        </header>
    )
}

export default Header
